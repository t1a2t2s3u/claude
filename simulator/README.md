# 株式投資シミュレータ(ペーパートレード)

実在の株価(Yahoo Finance)を使って、実口座なしで売買判断を検証するシミュレータです。
セットアップと使い方は実装完了後に追記します。

```bash
cd simulator
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```
